<div class="h-screen flex items-center justify-center">
  <div class="card w-full max-w-lg bg-gradient-to-b from-secondary to-base-100 shadow-xl p-8 flex items-center justify-center flex-col space-y-8">
    <h2 class="font-mplus text-xl md:text-2xl font-bold"><%= t('devise.registrations.new.title') %></h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "flex flex-col space-y-8 items-center" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <!-- 名前 -->
      <div class="form-control w-full max-w-md">
        <label class="label">
          <span class="label-text font-semibold"><%= t('devise.shared.name') %></span>
        </label>
        <%= f.text_field :name, placeholder: t('devise.shared.placeholders.name'), class: "input input-primary w-full" %>
      </div>

      <!-- メールアドレス -->
      <div class="form-control w-full max-w-md">
        <label class="label">
          <span class="label-text font-semibold"><%= t('devise.shared.email') %></span>
        </label>
        <%= f.email_field :email, placeholder: t('devise.shared.placeholders.email'), class: "input input-primary w-full" %>
      </div>

      <!-- パスワード -->
      <div class="form-control w-full max-w-md">
        <label class="label">
          <span class="label-text font-semibold"><%= t('devise.shared.password') %></span>
        </label>
        <% if @minimum_password_length %>
          <em><%= t('devise.shared.minimum_password_length', count: @minimum_password_length) %></em>
        <% end %>
        <%= f.password_field :password, placeholder: t('devise.shared.placeholders.password'), class: "input input-primary w-full" %>
      </div>

      <!-- パスワード（確認用） -->
      <div class="form-control w-full max-w-md">
        <label class="label">
          <span class="label-text font-semibold"><%= t('devise.shared.password_confirmation') %></span>
        </label>
        <%= f.password_field :password_confirmation, placeholder: t('devise.shared.placeholders.password_confirmation'), class: "input input-primary w-full" %>
      </div>

      <div class="actions">
        <%= f.submit t('devise.registrations.new.sign_up'), class: "btn btn-primary font-mplus w-full max-w-md px-10" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>