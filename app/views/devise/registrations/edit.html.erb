<div class="flex flex-col items-center max-w-5xl mx-auto px-4 mt-8">
  <h2 class="text-2xl font-bold mb-6">プロフィール編集</h2>

  <%= form_with model: resource, url: registration_path(resource_name), method: :put, local: true, multipart: true do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <!-- アバター -->
    <div class="flex flex-col items-center w-full space-y-6">
      <%= f.label :avatar, class: "font-mdium" %>

      <% if resource.avatar.attached? %>
        <div class="mt-2">
          <%= image_tag resource.avatar.variant(:thumb), class: "rounded-full mt-1" %>
        </div>
      <% else %>
        <%= avatar_placeholder(resource) %>
      <% end %>

      <%= f.file_field :avatar, class: "file-input" %>
    </div>


    <!-- ユーザー名 -->
    <div class="w-full mt-8">
      <%= f.label :name, class: "font-medium mb-1" %>
      <%= f.text_field :name, class: "input input-bordered w-full" %>
    </div>

    <div class="w-full mt-8">
      <%= f.label :introduction, class: "font-medium mb-1" %>
      <%= f.text_field :introduction, placeholder: "自己紹介文２００文字まで", class: "textarea py-24 w-full" %>
    </div>

    <div class="w-full mt-8">
      <%= f.submit "更新する", class: "btn btn-primary w-full" %>
    </div>
  <% end %>
