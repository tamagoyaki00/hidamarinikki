<div class= "flex flex-col items-center max-w-5xl mx-auto px-4 mt-8">
  <h2 class="highlight text-2xl mb-8">直近12カ月の振り返り</h2>
  <ul class="space-y-4">
      <% @months.each do |month| %>
      <li class="text-xl">
          <%= link_to month.strftime("%Y年%m"),
                      monthly_review_path(month: month.strftime("%Y-%m")) %>
      </li>
      <% end %>
  </ul>
</div>